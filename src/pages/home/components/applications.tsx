import * as React from "react";
import { Container, Row, Col } from "react-bootstrap";

interface Props {}

const Applications: React.SFC<Props> = props => {
	return (
		<Container>
			<Row>
				<Col />
				<Col />
				<Col />
				<Col />
			</Row>
		</Container>
	);
};
